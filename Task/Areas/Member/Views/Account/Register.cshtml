@model global::Task.Areas.Member.ViewModels.MemberRegisterVm
@{
    ViewData["Title"] = "إنشاء حساب";
}

<section class="mx-auto max-w-3xl">
    <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8">
        <h2 class="text-2xl font-extrabold text-slate-900">إنشاء حساب عضو جديد</h2>
        <p class="mt-2 text-sm text-slate-500">املأ البيانات التالية لإنشاء حسابك في المتجر.</p>

        <form asp-area="Member" asp-controller="Account" asp-action="Register" method="post" class="mt-6 space-y-4">
            <div asp-validation-summary="All" class="rounded-xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700"></div>

            <div>
                <label asp-for="FullName" class="mb-1 block text-sm font-bold text-slate-700">الاسم الكامل</label>
                <input asp-for="FullName" class="w-full rounded-xl border border-slate-300 px-4 py-3 text-sm outline-none transition focus:border-brand-500 focus:ring-2 focus:ring-brand-100" placeholder="اكتب اسمك الكامل" />
                <span asp-validation-for="FullName" class="mt-1 block text-xs font-semibold text-rose-600"></span>
            </div>

            <div>
                <label asp-for="Email" class="mb-1 block text-sm font-bold text-slate-700">البريد الإلكتروني</label>
                <input asp-for="Email" dir="ltr" class="w-full rounded-xl border border-slate-300 px-4 py-3 text-sm outline-none transition focus:border-brand-500 focus:ring-2 focus:ring-brand-100" placeholder="name@example.com" />
                <span asp-validation-for="Email" class="mt-1 block text-xs font-semibold text-rose-600"></span>
            </div>

            <div>
                <label asp-for="Password" class="mb-1 block text-sm font-bold text-slate-700">كلمة المرور</label>
                <input asp-for="Password" type="password" dir="ltr" class="w-full rounded-xl border border-slate-300 px-4 py-3 text-sm outline-none transition focus:border-brand-500 focus:ring-2 focus:ring-brand-100" placeholder="6 أحرف على الأقل" />
                <span asp-validation-for="Password" class="mt-1 block text-xs font-semibold text-rose-600"></span>
            </div>

            <button type="submit" class="w-full rounded-xl bg-brand-700 px-4 py-3 text-sm font-bold text-white transition hover:bg-brand-900">إنشاء الحساب</button>
        </form>

        <p class="mt-5 text-sm text-slate-600">
            لديك حساب بالفعل؟
            <a asp-area="" asp-controller="Account" asp-action="Login" class="font-extrabold text-brand-700 hover:text-brand-900">تسجيل الدخول</a>
        </p>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
